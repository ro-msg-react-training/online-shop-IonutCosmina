import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme =>({
    button: {
        margin: "5px 5px",
        
      },
      cartButton:{
          alignContent:'left',
      },
      link:{
        textDecoration: "none",
      }
  
    }));
    
