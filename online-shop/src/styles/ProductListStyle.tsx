import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme =>({
    button: {
        margin: "5px 5px",
        marginTop: "70px"
        
      },
      cartButton:{
          alignContent:'left',
      },
      link:{
        textDecoration: "none",
      }
  
    }));
    
